/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { DeleteTagRequest } from '../../models/delete-tag-request';
import { MutationResponse } from '../../models/mutation-response';

export interface ManualDeleteTagEverywhere$Params {
      body: DeleteTagRequest
}

export function manualDeleteTagEverywhere(http: HttpClient, rootUrl: string, params: ManualDeleteTagEverywhere$Params, context?: HttpContext): Observable<StrictHttpResponse<MutationResponse>> {
  const rb = new RequestBuilder(rootUrl, manualDeleteTagEverywhere.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<MutationResponse>;
    })
  );
}

manualDeleteTagEverywhere.PATH = '/api/manual/delete-tag-everywhere';
