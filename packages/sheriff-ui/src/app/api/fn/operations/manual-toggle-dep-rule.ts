/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { MutationResponse } from '../../models/mutation-response';
import { ToggleDepRuleRequest } from '../../models/toggle-dep-rule-request';

export interface ManualToggleDepRule$Params {
      body: ToggleDepRuleRequest
}

export function manualToggleDepRule(http: HttpClient, rootUrl: string, params: ManualToggleDepRule$Params, context?: HttpContext): Observable<StrictHttpResponse<MutationResponse>> {
  const rb = new RequestBuilder(rootUrl, manualToggleDepRule.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<MutationResponse>;
    })
  );
}

manualToggleDepRule.PATH = '/api/manual/toggle-dep-rule';
